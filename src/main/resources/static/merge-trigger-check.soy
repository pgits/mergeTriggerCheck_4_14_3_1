{namespace com.dell.bitbucket.merge.checks.hook.mergetriggercheck}

/**
 * @param? config
 * @param? errors
 */
{template .view}
    {call aui.form.textField}
        {param id: 'reviewers' /}
        {param value: $config ? $config['reviewers'] : null /}
        {param labelContent: 'reviewers'  /}
        {param descriptionText: 'Some description for reviewers' /}
        {param errorTexts: $errors ? $errors['reviewers'] : null /}
    {/call}
{/template}
